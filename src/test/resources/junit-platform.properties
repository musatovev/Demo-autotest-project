# Enable parallel test run if multiple test classes exist.
# when one thread is working on a file, that file is locked from changes,
# so the service returns a response code LOCKED = 423
junit.jupiter.execution.parallel.enabled = false
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.mode.classes.default = concurrent